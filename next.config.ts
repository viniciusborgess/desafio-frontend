import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['img.mbras.com.br', 'images.unsplash.com', 'www.donaizabel.com.br'],
  },
  /* config options here */
};

export default nextConfig;
